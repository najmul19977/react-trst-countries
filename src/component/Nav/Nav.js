import React from 'react';
import './Nav.css'

const Nav = () => {
    return (
        <div>
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Blog</a>
            <a href="#">Profile</a>
        </div>
    );
};

export default Nav;